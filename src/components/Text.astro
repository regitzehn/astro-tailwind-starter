---
// Henter parametrene fra Astro.props
const { teksttype = "brødtekst", stylingclasses = "" } = Astro.props;

//Definer den styling (Tailwind classes), der gør sig gældende for de forskellige størrelser.
const sizeClasses = {
  konsultativ: "font-gruppo text-sm text-italic",
  brødtekst: "text-gray-800 font-quantico w-128 sm:w-96 md:w-128",
};
---

<p class:list={[sizeClasses[teksttype], stylingclasses]}><slot /></p>
